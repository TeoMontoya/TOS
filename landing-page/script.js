function handleImageError(image) {
    image.style.display = 'none'; // Hide the broken image
    // Alternatively, you can replace the broken image with a placeholder
    // image.src = 'path/to/placeholder.jpg';
  }